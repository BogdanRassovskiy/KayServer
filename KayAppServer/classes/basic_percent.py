class basic_percent:
    changed = "1"
    removed = "0"
    #
    var = "var"
    cur = "cur"
    per = "per"
    def __init__(self,
        var,
        cur,
        per,
        ):
        self.var = var
        self.cur = cur
        self.per = per
